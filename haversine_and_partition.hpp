#ifndef haversine_and_partition_H
#define haversine_and_partition_H

#include "data_structures.hpp"

extern double distanceEarth(Point, Point);
bool liesInPartition(Partition, Point);

#endif // !haversine_and_partition_H